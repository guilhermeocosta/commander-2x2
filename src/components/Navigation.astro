---
import { navigationItems } from "../config/navigation";

interface Props {
  showIcons?: boolean;
  itemClass?: string;
  listClass?: string;
}

const { showIcons = false, itemClass = "", listClass = "" } = Astro.props;
---

<ul class={listClass}>
  {
    navigationItems.map((item) => (
      <li>
        <a href={item.href} class={itemClass}>
          {showIcons && <item.icon class="size-4 mr-2" aria-hidden="true" />}
          {item.label}
        </a>
      </li>
    ))
  }
</ul>
